/** The message map is browser-only; this Host entry keeps the DSH bundle shape valid. */
export const name = 'dsh-codex-timeline'

export function apply(): void {
  // The client half observes already-rendered conversation nodes.
}
