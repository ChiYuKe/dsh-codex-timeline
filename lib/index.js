//#region src/index.ts
/** The message map is browser-only; this Host entry keeps the DSH bundle shape valid. */
const name = "dsh-codex-timeline";
function apply() {}
//#endregion
export { apply, name };
