//#region src/index.ts
/** The message map is browser-only; this Host entry keeps the DSH bundle shape valid. */
const name = "dsh-message-map";
function apply() {}
//#endregion
export { apply, name };
